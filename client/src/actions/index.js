import axios from 'axios';
import History from '../history'

export const reviewBook = (review) => async dispatch => {

    // const products = await axios.post('/book/compareBook',school);
    dispatch({
        type: "COMPARE_BOOK",
        payload: {}
    })
}






